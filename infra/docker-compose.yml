version: '3.8'
services:
  db-PhotoSession912:
    image: postgres:16-alpine
    container_name: postgres-photo-session912
    env_file:
      - .env
    ports:
      - "5544:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: